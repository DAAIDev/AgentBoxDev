{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "git pull --rebase --autostash 2>&1 | head -5 || echo 'git pull skipped (no network or not a git repo)'"
          }
        ]
      }
    ]
  }
}
